!(function (factory) {
  "function" == typeof define && define.amd
    ? define(["jquery"], factory)
    : "object" == typeof exports
    ? (module.exports = factory(require("jquery")))
    : factory(jQuery);
})(function ($) {
  var dispatch = $.event.dispatch || $.event.handle,
    special = $.event.special,
    uid1 = "D" + +new Date(),
    uid2 = "D" + (+new Date() + 1);
  (special.scrollstart = {
    setup: function (data) {
      var timer,
        _data = $.extend({ latency: special.scrollstop.latency }, data),
        handler = function (evt) {
          var _self = this,
            _args = arguments;
          timer ? clearTimeout(timer) : ((evt.type = "scrollstart"), dispatch.apply(_self, _args)),
            (timer = setTimeout(function () {
              timer = null;
            }, _data.latency));
        };
      $(this).bind("scroll", handler).data(uid1, handler);
    },
    teardown: function () {
      $(this).unbind("scroll", $(this).data(uid1));
    },
  }),
    (special.scrollstop = {
      latency: 250,
      setup: function (data) {
        var timer,
          _data = $.extend({ latency: special.scrollstop.latency }, data),
          handler = function (evt) {
            var _self = this,
              _args = arguments;
            timer && clearTimeout(timer),
              (timer = setTimeout(function () {
                (timer = null), (evt.type = "scrollstop"), dispatch.apply(_self, _args);
              }, _data.latency));
          };
        $(this).bind("scroll", handler).data(uid2, handler);
      },
      teardown: function () {
        $(this).unbind("scroll", $(this).data(uid2));
      },
    });
});


/*
     FILE ARCHIVED ON 03:33:41 Mar 18, 2024 AND RETRIEVED FROM THE
     INTERNET ARCHIVE ON 16:19:20 Apr 29, 2026.
     JAVASCRIPT APPENDED BY WAYBACK MACHINE, COPYRIGHT INTERNET ARCHIVE.

     ALL OTHER CONTENT MAY ALSO BE PROTECTED BY COPYRIGHT (17 U.S.C.
     SECTION 108(a)(3)).
*/
/*
playback timings (ms):
  capture_cache.get: 0.836
  load_resource: 217.713
  PetaboxLoader3.resolve: 105.454
  PetaboxLoader3.datanode: 95.327 (2)
  loaddict: 45.475
*/